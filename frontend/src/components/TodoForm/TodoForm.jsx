import React from 'react';
import PropTypes from 'prop-types';

const TodoForm = () => {
    return(
        <div>
            Form
        </div>
    );
};

TodoForm.propTypes = {

};

export default TodoForm;